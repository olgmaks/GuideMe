package com.epam.gm.services;

import java.util.List;


import java.util.Properties;

import javax.mail.Message;
import javax.mail.MessagingException;
import javax.mail.Session;
import javax.mail.Transport;
import javax.mail.internet.InternetAddress;
import javax.mail.internet.MimeMessage;

import com.epam.gm.model.Event;
import com.epam.gm.olgmaks.absractdao.dbcontrol.ConnectionManager;
import com.epam.gm.olgmaks.absractdao.dbcontrol.MyConnection;
import com.epam.gm.olgmaks.absractdao.general.AbstractDao;
import com.epam.gm.olgmaks.absractdao.general.IDao;

public class UserService {
	public static void main(String[] args) {
		/*
		 * System.out.println("test start"); IDao<Event> abstractDao = new
		 * AbstractDao<Event>( MyConnection.getConnection(), Event.class);
		 * 
		 * List<Event> list = abstractDao.getAll(); System.out.println(list);
		 * 
		 * for (Event e : list) { System.out.println(e); }
		 */


	}

}
